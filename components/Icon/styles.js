import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
    container : {
        marginRight : 5,
        flexDirection : 'row',
        alignItems : 'center',
    }
})

export default styles;